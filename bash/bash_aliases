# bash_aliases

# GitHub CLI alias
# alias ghx='/usr/local/bin/gh'

# Reload shell session
alias reload='source ~/.bash_profile'

# Fix vscode openning multiple dock icons
alias code='open -b com.microsoft.VSCode "$@"'
